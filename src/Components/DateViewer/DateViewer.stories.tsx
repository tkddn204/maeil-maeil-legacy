import React from 'react';
import { Meta, Story } from '@storybook/react/types-6-0';
import DateViewer from '.';

export default {
  title: 'Components/DateViewer',
  component: DateViewer,
} as Meta;

export const DateViewerComponent: Story = () => <DateViewer />;
